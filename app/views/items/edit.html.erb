<h1>編集画面</h1>
<%= form_with model:@item, url:"/items/#{@item.id}", local:true do |f| %>
  <h4>商品名</h4>
  <%= f.text_field :name %>
  <h4>商品紹介</h4>
  <%= f.text_area :caption %>
  <%= f.submit '保存' %>
<% end %>